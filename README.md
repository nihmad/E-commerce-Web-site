## Backend Django e-commerce

Ce dossier contient le backend Django pour le site e-commerce.

- Framework: Django + Django REST Framework
- Base de données: PostgreSQL
- Cache: Redis
- Paiement: Stripe

Pour démarrer (une fois les dépendances installées) :

```bash
cd backend
python manage.py migrate
python manage.py runserver
```

